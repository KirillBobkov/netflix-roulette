import React from 'react';
import { NotFound, NotFoundMessage } from './NotFound.styles';

export default () => (
  <NotFound>
    <NotFoundMessage>
      Page not found!
    </NotFoundMessage>
  </NotFound>
);
